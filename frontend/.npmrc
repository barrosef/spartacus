# Necessário para React Native / Expo: o Gradle resolve react-native via
# require.resolve() a partir do diretório android/, o que não funciona com
# o virtual store do pnpm. node-linker=hoisted cria um node_modules flat
# (como npm), permitindo a resolução nativa dos pacotes.
node-linker=hoisted
